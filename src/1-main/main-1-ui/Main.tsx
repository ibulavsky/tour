import React from 'react';
import Header from "./Header";
import Routes from "./Routes";

// header and routes
const Main: React.FC = () => {
    return (
        <div>
            <Header/>
            <Routes/>
        </div>
    );
};

export default Main;
